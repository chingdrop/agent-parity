# Applied automatically by `docker compose up` — exposes MinIO's API and
# web console for local inspection (http://localhost:9001, login with
# MINIO_ROOT_USER/PASSWORD).

services:
  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
