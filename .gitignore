# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# CLI output — stray CSVs are ignored everywhere except the committed fixtures
output/
*.log
*.csv
!sample_data/**/*.csv

# `run` subcommand's default run-history store (see src/agent_parity/scheduling/db.py) —
# AGENT_PARITY_DB_URL points elsewhere for anything that isn't local/demo.
agent_parity.db
*.db

# Secrets — config.yaml holds only ${VAR} references and is safe to commit;
# .env holds the actual values and must never be.
.env
.env.*
!.env.example

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.claude/
.idea/
.vscode/
.DS_Store
